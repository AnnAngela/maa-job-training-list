{
  "name": "maa-job-training-list",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "分析萨拉托加 MAA 作业练度要求并生成培养优先级清单的静态站点",
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "test": "npm run lint && vitest run --coverage",
    "test:watch": "vitest",
    "generate:data": "node scripts/run-generate-data.js"
  },
  "engines": {
    "node": "^22.21 || ^24.11"
  },
  "devDependencies": {
    "@annangela/eslint-config": "^15.0.0",
    "@vitest/coverage-v8": "^5.0.1",
    "eslint": "^10.11.0",
    "jsdom": "^30.1.0",
    "vitest": "^5.0.1"
  }
}
